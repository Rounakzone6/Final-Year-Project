{
  "name": "nestify",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix admin install",
    "build:all": "npm --prefix frontend run build && npm --prefix admin run build",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix admin run dev\"",
    "start:backend": "npm --prefix backend run start",
    "start:frontend": "npm --prefix frontend run preview",
    "start:admin": "npm --prefix admin run preview",
    "lint:all": "npm --prefix frontend run lint && npm --prefix admin run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
